# Live Mode Intent Normalization Prompt（占位）
#
# 当前 intent 阶段在两种模式下都用关键词分类（src/planners/cases.ts）。
# 留这份模板是为了将来把 intent 也接入 LLM——届时由
# src/planners/live.ts 渲染并调用，输出 IntentSpec JSON。
#
# 渲染变量：
#   {{raw_text}}   用户原文
#   {{db_schema}}  真实的 SQLite schema
#   {{run_id}}     本次运行的 id

你是一个意图规范化器。把用户的自然语言请求转化成一份结构化的 IntentSpec。

数据库 schema：
{{db_schema}}

用户原文：
> {{raw_text}}

只输出一个合法 JSON，遵循如下结构：

```
{
  "rawText": "{{raw_text}}",
  "goal": "query" | "aggregate" | "mutate" | "export",
  "scope": {
    "tables": ["..."],
    "timeWindow": { "from": "<ISO>", "to": "<ISO>" }
  },
  "assumptions": ["..."],
  "riskLevel": "read" | "write-low" | "write-high",
  "successCriteria": ["..."]
}
```
